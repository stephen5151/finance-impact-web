# 模型 API key 接口 —— 填入后自动启用混合问答，不填则降级到关键词匹配。
# 本地：复制为 .env.local 并填写；线上：在 Vercel 项目 Settings → Environment Variables 配置。

# 必填：填了才启用模型
LLM_API_KEY=

# 可选：anthropic（默认）| openai
# openai 兼容协议同样适用于 DeepSeek、通义千问、智谱 GLM —— 配合 LLM_BASE_URL 使用
LLM_PROVIDER=anthropic

# 可选：默认 anthropic=claude-opus-4-8，openai=gpt-4o-mini
# LLM_MODEL=claude-opus-4-8

# 可选：自定义 API 地址（自部署或第三方 OpenAI 兼容服务）
# 例：DeepSeek -> https://api.deepseek.com/v1/chat/completions
# LLM_BASE_URL=


# ===== 最新动态：自动抓取新闻（B 线）=====
# 三者都配齐后，定时任务才会抓取并发布最新动态；缺任意一项则该区块不显示，页面不报错。

# 存储后端：GitHub Gist（免费、无需绑卡）。需要下面两个：
# 1) GITHUB_TOKEN：有 gist 读写权限的 GitHub Token
#    （fine-grained 勾 Gists: Read and write，或 classic token 勾 gist scope）
# 2) GIST_ID：新建一个 secret gist（放个占位文件即可），
#    URL 里 gist.github.com/<用户名>/<这一段> 就是 ID
GITHUB_TOKEN=
GIST_ID=

# 定时任务密钥。自己设一个随机字符串，Vercel Cron 会自动带上它来鉴权，
# 防止 /api/cron/refresh-news 被公开调用。
CRON_SECRET=

# 可选：RSS 源，逗号分隔。不填则用代码里的默认源（已实测当天更新、可直连）：
#   美联储 / BBC / 卫报 / CNBC（英文，自动翻译成中文）+ 华尔街见闻（中文）
# 只有命中可信源白名单（src/data/news.ts）的条目才会自动发布。
# NEWS_FEEDS=https://www.federalreserve.gov/feeds/press_all.xml,http://feeds.bbci.co.uk/news/business/rss.xml,https://dedicated.wallstreetcn.com/rss.xml
